{
  "name": "foundation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./tailwind.input.css -o ./dist/style.css --minify",
    "watch": "tailwindcss -i ./tailwind.input.css -o ./dist/style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
